definitions:
  Text:
    description: 注明这项设定的名字对应的本地化文本。
    type: object
    additionalProperties: false
    properties:
      text:
        type: string
      locale:
        type: string
        enum:
        - Locale.Chs
        - Locale.Eng
        default: Locale.Chs
